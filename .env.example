# Activa el modo Backend real (SQL Server vía Express) cuando esté en true.
# Si está en false, se utilizará el LocalStorage del navegador para desarrollo sin base de datos.
VITE_USE_API=false
VITE_API_URL=http://localhost:3001/api

